services:
  app:
    build: .
    ports:
      - "7860:7860"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - .:/workspace
    working_dir: /workspace
    command: python app.py